# Dependencies & build artifacts (installed/built inside the image)
**/node_modules
frontend/.next
backend/dist
frontend/tsconfig.tsbuildinfo

# Local SQLite database — persisted via a mounted volume in prod, never baked in
backend/data

# Secrets & local env (but keep the .env.example templates)
**/.env
**/.env.local
!**/.env.example

# VCS & misc
.git
.gitignore
*.log
npm-debug.log*
.DS_Store
